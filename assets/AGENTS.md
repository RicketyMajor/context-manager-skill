# Antigravity Rules for this Workspace

This file is automatically read by Antigravity at the start of every session (equivalent to CLAUDE.md). It contains persistent rules, context pointers, and strict guidelines for this specific project.

## Core Directives
1. **Context Management:** This project uses the `context-manager` skill. All architectural decisions, specs, and handoffs are stored in the `/context` directory.
2. **Auto-Documentation:** Never wait to be asked. Always update the documentation in `/context` immediately after making significant changes to the codebase.
3. **Single Source of Truth:** Do not hallucinate project goals. Read `/context/general/overview.md` if you need the big picture.

## Project-Specific Rules
*(The Context-Manager agent will append project-specific rules here as they are discovered or decided upon)*
